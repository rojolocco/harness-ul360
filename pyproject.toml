[project]
name = "harness-ul360"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [{ name = "rojolocco", email = "rojolocco@gmail.com" }]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
harness-ul360 = "scripts.main:main"

[tool.uv.build-backend]
module-root = "."
module-name = "scripts"

[build-system]
requires = ["uv_build>=0.12.7,<0.13.0"]
build-backend = "uv_build"
